// Pseudocode voor het uitvoeren van een training
function StartTrainingPage() {
  const [templates, setTemplates] = useState([]);
  const [selectedTemplate, setSelectedTemplate] = useState(null);
  const [logData, setLogData] = useState({});

  const handleStartTraining = (templateId) => {
    // Haal het geselecteerde schema op en initialiseer logData
  };

  const handleSaveLog = async () => {
    // Sla de trainingssessie op in Supabase
  };

  return (
    <div>
      {/* Lijst van trainingsschema's */}
      {/* Formulier voor het loggen van de training */}
    </div>
  );
}
